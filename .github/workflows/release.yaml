name: Release CEF Artifacts

on:
  push:
    branches:
      - release/v*

defaults:
  run:
    shell: bash

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            url: "https://cef-builds.spotifycdn.com/cef_binary_132.3.2%2Bg4997b2f%2Bchromium-132.0.6834.161_windows64_minimal.tar.bz2"
            artifact_name: cef-windows-x86_64
            extract_cmd: "7z x"
            compress_cmd: "7z a -tzip"
            
          - os: macos-latest
            url: "https://cef-builds.spotifycdn.com/cef_binary_132.3.2%2Bg4997b2f%2Bchromium-132.0.6834.161_macosarm64_minimal.tar.bz2"
            artifact_name: cef-macos-arm64
            extract_cmd: "tar -xjf"
            compress_cmd: "zip -r"
            
          - os: ubuntu-latest
            url: "https://cef-builds.spotifycdn.com/cef_binary_132.3.2%2Bg4997b2f%2Bchromium-132.0.6834.161_linux64_minimal.tar.bz2"
            artifact_name: cef-linux-x86_64
            extract_cmd: "tar -xjf"
            compress_cmd: "zip -r"
            
    runs-on: ${{ matrix.os }}
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Create artifacts directory
      run: mkdir artifacts
      
    - name: Download CEF build
      run: |
        curl -L --http1.1 "${{ matrix.url }}" -o artifacts/cef.tar.bz2
          
    - name: Extract archive
      run: |
        cd artifacts
        ${{ matrix.extract_cmd }} cef.tar.bz2    
        if [ "${{ matrix.os }}" = "windows-latest" ]; then
          ${{ matrix.extract_cmd }} *.tar
        fi
        mv cef_binary_*/* .
        
    - name: Process CEF artifacts
      run: cargo cef-artifacts
        
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.artifact_name }}
        path: artifacts/cef/*
  # release:
  #   needs: [build]
  #   runs-on: ubuntu-latest
  #   steps:
  #   - name: Download all artifacts
  #     uses: actions/download-artifact@v4
  #     with:
  #       path: ./artifacts
        
  #   - name: Create GitHub Release
  #     env:
  #       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #     run: |
  #       # Get the tag name or create one based on timestamp
  #       if [[ $GITHUB_REF == refs/tags/* ]]; then
  #         TAG_NAME=${GITHUB_REF#refs/tags/}
  #       else
  #         TAG_NAME="cef-132.3.2-$(date +%Y%m%d-%H%M%S)"
  #       fi
        
  #       gh release create "$TAG_NAME" \
  #         --title "CEF 132.3.2 Artifacts" \
  #         --notes "Pre-built CEF 132.3.2 artifacts for Windows x86_64, macOS ARM64, and Linux x86_64" \
  #         ./artifacts/cef-windows-x86_64/cef-windows-x86_64.zip \
  #         ./artifacts/cef-macos-arm64/cef-macos-arm64.zip \
  #         ./artifacts/cef-linux-x86_64/cef-linux-x86_64.zip